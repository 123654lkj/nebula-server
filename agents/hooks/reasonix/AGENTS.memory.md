# Reasonix × 星枢记忆门禁（摘录）

你接入 **Nebula L2**（默认 `http://127.0.0.1:26670`）。

## 硬门禁
1. 工程/infra/「上次」→ 先 `POST /v5/bootstrap` 或 `/ask`，无召回不改代码
2. 只用 contract/bootstrap/pack，禁止 dump 全文 hits
3. `executable=false` / superseded → 不当现行
4. 有 readback → 回读 L1 笔记原文
5. 密钥禁止写入向量；纠正 → add/supersede

## 技能名（若已安装）
workflow-discipline · ponytail · recall-before-code · verify-before-assert · correction-capture · nebula-recall
