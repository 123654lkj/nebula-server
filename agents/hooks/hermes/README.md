# Hermes 适配

1. Skills → `~/.hermes/skills/<name>/SKILL.md`（与五门同名）
2. 会话启动：Hermes 若支持 hook/bootstrap 命令，指向：
   `bash ~/.nebula/hooks/session-start-memory.sh`
3. 合并 `AGENTS.memory.md` 到 Hermes 全局指令

Hermes 与 Grok/Codex 共用同一套 skill 语义；只换安装路径。
