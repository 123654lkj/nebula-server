# 部署与同步

```bash
# 一键（推荐 Docker）
cp .env.example .env   # 填 BAILIAN_API_KEY
npm run docker:up

# 或本机 Python
npm run setup && npm start

# 同步笔记 → 星枢
npm run sync
```

定时同步示例见 `deploy/vault-nebula-sync.timer`。
